#SLURM cluster config
# module load Anaconda3
# conda activate my-env

# Your code begins here
#gatk --java-options "-Xmx31g" ModelSegments \
 #  --denoised-copy-ratios sandbox3/Anorm.denoisedCR.tsv \
 #  --allelic-counts sandbox3/AG12NB1.allelicCounts.tsv \
 #  --output sandbox3/sandbox \
 #  --output-prefix Anormal


#gatk CallCopyRatioSegments \
 #       --I sandbox3/sandbox/Anormal.cr.seg \
 #      --O sandbox3/sandbox/Anormal.called.seg


modelsegments:
	partition: highmem
	mem: 31G
	ncpus: 4
	ntasks:1 

denoisereadcounts:
	partition: highmem
	mem: 15G
	ncpus: 4
	ntasks: 1


#gatk --java-options "-Xmx15g" DenoiseReadCounts \
  #  -I sandbox3/Anorm.counts.hdf5 \
  #  --count-panel-of-normals sandbox3/ANpon.pon.hdf5 \
  #  --standardized-copy-ratios sandbox3/Anorm.standardizedCR.tsv \
  #  --denoised-copy-ratios sandbox3/Anorm.denoisedCR.tsv


 
